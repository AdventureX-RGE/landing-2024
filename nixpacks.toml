[variables]
NODE_ENV = "development"

[phases.setup]
nixPkgs = ["nodejs_18", "yarn-1_x"]

[phases.install]
cmds = [
  "corepack enable",
  "yarn install --frozen-lockfile"
]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "yarn serve"
